# Secrets — copy this file to .env and set real values:
#     cp .env.example .env
#
# These are SENSITIVE values. In Kubernetes they map to a Secret
# (never a ConfigMap). .env is gitignored so secrets are not committed.

# Password for the Redis backing service. Used by both the app and Redis.
REDIS_PASSWORD=change-me-in-real-deployments
